using System;
using System.Collections.Generic;
using System.Text;

namespace MyNewPlayground.Class.CH2
{
    internal class _2
    {
/*
void Algorithm(int n)
{
    int a = 0;                   // O(1)。a 是一个普通整数，不管 n 是多少，它永远只占 1 个整数的空间。空间复杂度：O(1)（常数阶，和 n 无关）
    int[] b = new int[10000];    // O(1)。b 是一个固定长度为 10000 的数组。虽然它占了 10000 个整数的空间，看起来很大，但它的大小和 n 完全没有关系，是一个写死的常数。空间复杂度：O(1)（常数阶，和 n 无关）
    if (n > 10)
    {
        int[] nums = new int[n]; // O(n)。nums 是一个长度为 n 的数组。n 越大，它占的内存越多，和 n 成正比。空间复杂度：O(n)（线性阶，随 n 增长）
    }
}

int Function()
{
    return 0;
}
void Loop(int n)
{
    for (int i = 0; i < n; i++)
    {
        Function(); // 完全合法！C# 允许你调用一个有返回值的函数，但选择忽略它的返回值。
    }
}
int Recur(int n)
{
    if (n == 1)
    {
        return 1;
    }
    return Recur(n - 1);
}
*/
    }
}
