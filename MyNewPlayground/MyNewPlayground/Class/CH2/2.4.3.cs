using System;
using System.Collections.Generic;
using System.Text;

namespace MyNewPlayground.Class.CH2
{
    internal class _243
    {
/*
// 平方阶。平方阶常见于矩阵和图。
using hello_algo.utils;

void Quadratic(int n)
{
    int[,] numMatrix = new int[n, n];
    List<List<int>> numList = [];
    for (int i = 0; i < n; i++)
    {
        List<int> tmp = [];
        for (int j = 0; j < n; j++)
        {
            tmp.Add(i);
        }
        numList.Add(tmp);
    }
}

//平方阶递归实现。
int QuadraticRecur(int n)
{
    if (n <= 0)
    {
        return 0;
    }
    int[] nums = new int[n];
    Console.WriteLine("递归 n = " + n + " 中的 nums 长度 = " + nums.Length);
    return QuadraticRecur(n - 1);
}

QuadraticRecur(10);

//指数阶。常见于二叉树。
TreeNode BuildTree(int n)
{
    if (n == 0)
    {
        return null;
    }
    TreeNode root = new(0)
    {
        left = BuildTree(n - 1),
        right = BuildTree(n - 1)
    };
    return root;
}
*/
    }
}
