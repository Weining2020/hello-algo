using System;
using System.Collections.Generic;
using System.Text;

namespace MyNewPlayground.Class.CH8
{
    internal class _822
    {/*
public MaxHeap(IEnumerable<int> num)
{
    maxHeap = new List<int>(num);

    int size = Parent(Size() - 1);
    for (int i = size; i >= 0; i--)
    {
        SiftDown(i);
    }
}
*/
    }
}
