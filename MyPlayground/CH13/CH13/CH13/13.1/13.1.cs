/*
using hello_algo.utils;
List<TreeNode> res = []; //教程上没加这个，我加在这里防止报错。
void PreOrder(TreeNode? root)
{
    if(root == null)
    {
        return;
    }
    if(root.val == 7)
    {
        res.Add(root);
    }
    PreOrder(root.left);
    PreOrder(root.right);

}

*/